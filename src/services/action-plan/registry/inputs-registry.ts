import { birthdayAdapter } from '../input-adapters/birthdays-adapter';
import { needsFollowUpAdapter } from '../input-adapters/general-follow-up';

export const inputAdaptersRegistry = [birthdayAdapter, needsFollowUpAdapter];
